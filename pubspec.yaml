name: fitai_analyzer
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.10.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^4.5.0
  cloud_firestore: ^6.1.3
  firebase_auth: ^6.2.0
  riverpod: ^2.5.1
  flutter_riverpod: ^2.5.1
  http: ^1.2.2
  crypto: ^3.0.3 # HMAC-SHA1 per la firma OAuth1 del login Garmin lato client (nativo)
  flutter_web_auth_2: ^5.0.1
  # Usato solo per intercettare il sottoprocesso WebView2 in main() su desktop
  # (flutter_web_auth_2 lo usa per l'OAuth). Vedi lib/services/webview_subprocess_io.dart.
  desktop_webview_window: ^0.2.3
  shared_preferences: ^2.3.2
  url_launcher: ^6.2.5
  app_links: ^6.3.2
  flutter_secure_storage: ^10.0.0
  json_annotation: ^4.9.0
  fl_chart: ^1.2.0
  go_router: ^17.1.0
  intl: ^0.20.2
  google_generative_ai: ^0.4.7
  flutter_dotenv: ^5.1.0
  image_picker: ^1.0.7
  path_provider: ^2.1.4
  smooth_page_indicator: ^1.1.0
  flutter_svg: ^2.0.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  json_serializable: ^6.8.0
  build_runner: ^2.4.7
  flutter_launcher_icons: ^0.14.3

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - .env
    - assets/branding/app_icon.png
    - assets/icons/
    - assets/illustrations/
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # Font del redesign "NaturaVita / Magical Natural UI" — bundlati offline
  # (nessuna dipendenza di rete: niente google_fonts runtime fetch).
  # ShantellSans = sans "hand-drawn UI" leggibile (titoli/testi/navigazione).
  # Caveat       = manoscritto (nomi utente, saluti). Vedi AppFonts in app_theme.dart.
  fonts:
    - family: ShantellSans
      fonts:
        - asset: assets/fonts/ShantellSans-Regular.ttf
        - asset: assets/fonts/ShantellSans-Medium.ttf
          weight: 500
        - asset: assets/fonts/ShantellSans-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/ShantellSans-Bold.ttf
          weight: 700
    - family: Caveat
      fonts:
        - asset: assets/fonts/Caveat-Regular.ttf
        - asset: assets/fonts/Caveat-Bold.ttf
          weight: 700

# Icona launcher: sorgente in assets/branding/app_icon.png → rigenera con:
#   dart run flutter_launcher_icons
flutter_launcher_icons:
  android: true
  ios: true
  image_path: assets/branding/app_icon.png
  adaptive_icon_background: "#000000"
  adaptive_icon_foreground: assets/branding/app_icon.png
  remove_alpha_ios: true
  background_color_ios: "#000000"
  web:
    generate: true
    image_path: assets/branding/app_icon.png
    background_color: "#000000"
    theme_color: "#000000"
  windows:
    generate: true
    image_path: assets/branding/app_icon.png
    icon_size: 256
  macos:
    generate: true
    image_path: assets/branding/app_icon.png
